name: ci
on:
  push:
    branches: [main, master]
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: test -d web && test -d server && test -f README.md
